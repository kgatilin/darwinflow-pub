# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# DarwinFlow CLI binary (only in root)
/dw
/dw-stable

# DarwinFlow data directory
.darwinflow/

# Backup files
*.backup
.agent/

# Claude Code local settings (only settings files)
.claude/*.json
.claude/*.backup
.darwinflow/claude-code.log
